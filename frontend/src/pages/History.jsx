import { useEffect, useMemo, useState } from "react";
import { apiDelete, apiGet, apiPatch } from "../api";
import { useAuth } from "../auth";
import { Alert, Modal, PageHeader, SearchableSelect, fmt } from "../components/Common";
import { useOptions } from "../optionsContext";
const MONTHS_TH=["มกราคม","กุมภาพันธ์","มีนาคม","เมษายน","พฤษภาคม","มิถุนายน","กรกฎาคม","สิงหาคม","กันยายน","ตุลาคม","พฤศจิกายน","ธันวาคม"];
export function EditHistory({row,options,onClose,onSaved}){const[quantity,setQuantity]=useState(row.quantity),[machineId,setMachineId]=useState(row.machine?(options.machines||[]).find(x=>x.code===row.machine)?.id||"":""),[requesterId,setRequesterId]=useState(row.requester_id||""),[recorderId,setRecorderId]=useState(row.recorder_id||""),[remark,setRemark]=useState(row.remark||""),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function save(e){e.preventDefault();setBusy(true);setError("");try{await apiPatch(`/history/${row.id}/`,{quantity,machine_id:machineId,requester_id:requesterId,recorder_id:recorderId,remark});onSaved()}catch(err){setError(err.message)}finally{setBusy(false)}}const employeeLabel=x=>x.name;return <Modal title="แก้ไขข้อมูล History" onClose={onClose}><form onSubmit={save}><div className="form-grid"><label className="field"><span>จำนวน *</span><input type="number" min="0.0001" step="any" value={quantity} onChange={e=>setQuantity(e.target.value)} required/></label><label className="field"><span>เครื่องจักร</span><SearchableSelect value={machineId} options={options.machines||[]} onChange={setMachineId} getLabel={x=>x.code} getSearchText={x=>`${x.code||""} ${x.name||""} ${x.location||""}`} placeholder="พิมพ์รหัส Machine"/></label><label className="field"><span>ผู้เบิก</span><SearchableSelect value={requesterId} options={options.employees||[]} onChange={setRequesterId} getLabel={employeeLabel} getSearchText={x=>`${x.employee_code||""} ${x.name||""} ${x.department||""}`} placeholder="พิมพ์ชื่อพนักงาน"/></label><label className="field"><span>ผู้บันทึก</span><SearchableSelect value={recorderId} options={options.employees||[]} onChange={setRecorderId} getLabel={employeeLabel} getSearchText={x=>`${x.employee_code||""} ${x.name||""} ${x.department||""}`} placeholder="พิมพ์ชื่อพนักงาน"/></label><label className="field span2"><span>Remark</span><textarea rows="3" value={remark} onChange={e=>setRemark(e.target.value)}/></label></div><Alert>{error}</Alert><div className="modal-actions"><button type="button" className="btn ghost" onClick={onClose}>ยกเลิก</button><button className="btn primary" disabled={busy}>บันทึก</button></div></form></Modal>}
function HistoryTable({rows,auth,onEdit,onDelete}){const canAction=auth.can("can_edit_history")||auth.can("can_delete_history"),head={position:"sticky",right:0,zIndex:4,background:"#f8fafc",boxShadow:"-8px 0 12px rgba(15,23,42,.04)"},cell={position:"sticky",right:0,zIndex:3,background:"#fff",boxShadow:"-8px 0 12px rgba(15,23,42,.04)"};return <div className="history-table-wrap" style={{overflowX:"auto",width:"100%"}}><table className="history-table"><thead><tr><th>วัน / เวลา</th><th>ประเภท</th><th>Part ID</th><th>Part Name</th><th>Part Detail</th><th>Maker</th><th>Location</th><th>เครื่องจักร</th><th>ผู้เบิก</th><th>ผู้บันทึก</th><th>จำนวน</th><th>Unit</th><th>Remark</th>{canAction&&<th style={head}>Action</th>}</tr></thead><tbody>{rows.map(r=><tr key={r.id}><td><b>{r.date}</b><small>{r.time}</small></td><td><span className={`status ${r.type==="IN"?"success":r.type==="OUT"?"danger":"warning"}`}>{r.type==="IN"?"รับเข้า":r.type==="OUT"?"เบิกออก":"ปรับยอด"}</span></td><td><b>{r.item_id}</b></td><td>{r.part_name}</td><td>{r.part_detail||"-"}</td><td>{r.maker||"-"}</td><td>{r.location||"-"}</td><td>{r.type==="OUT"?r.machine||"-":"-"}</td><td>{r.type==="OUT"?r.requester||"-":"-"}</td><td>{r.recorder||"-"}</td><td><b>{fmt(r.quantity)}</b></td><td>{r.unit}</td><td>{r.remark||"-"}</td>{canAction&&<td style={cell}><div className="history-actions" style={{display:"flex",gap:5,flexWrap:"nowrap"}}>{auth.can("can_edit_history")&&<button className="mini" onClick={()=>onEdit(r)}>แก้ไข</button>}{auth.can("can_delete_history")&&<button className="mini danger" onClick={()=>onDelete(r)}>ลบ</button>}</div></td>}</tr>)}</tbody></table></div>}
export default function History(){const auth=useAuth();const{options}=useOptions();const[rows,setRows]=useState([]),[type,setType]=useState("ALL"),[q,setQ]=useState(""),[requester,setRequester]=useState(""),[recorder,setRecorder]=useState(""),[error,setError]=useState(""),[edit,setEdit]=useState(null),[loading,setLoading]=useState(true),[openYears,setOpenYears]=useState({}),[openMonths,setOpenMonths]=useState({});async function load(){setLoading(true);setError("");try{const h=await apiGet(`/history/?${new URLSearchParams({type,q,requester_id:requester,recorder_id:recorder})}`);setRows(h.results||[])}catch(err){setError(err.message)}finally{setLoading(false)}}useEffect(()=>{load()},[type,requester,recorder]);const shown=useMemo(()=>rows.filter(x=>!q||`${x.item_id} ${x.part_name} ${x.part_detail} ${x.maker} ${x.machine} ${x.requester} ${x.recorder}`.toLowerCase().includes(q.toLowerCase())),[rows,q]);const groups=useMemo(()=>{const years=new Map();for(const row of shown){const d=new Date(row.transaction_date),valid=!Number.isNaN(d.getTime()),year=valid?String(d.getFullYear()):String(row.date||"").slice(-4),mi=valid?d.getMonth():0,key=`${year}-${String(mi+1).padStart(2,"0")}`;if(!years.has(year))years.set(year,new Map());const ms=years.get(year);if(!ms.has(key))ms.set(key,{key,label:MONTHS_TH[mi]||key,rows:[]});ms.get(key).rows.push(row)}return Array.from(years.entries()).sort((a,b)=>Number(b[0])-Number(a[0])).map(([year,months])=>({year,months:Array.from(months.values()).sort((a,b)=>b.key.localeCompare(a.key))}))},[shown]);useEffect(()=>{if(!groups.length)return;const y=groups[0],m=y.months[0];setOpenYears(p=>Object.keys(p).length?p:{[y.year]:true});if(m)setOpenMonths(p=>Object.keys(p).length?p:{[m.key]:true})},[groups]);async function remove(row){if(!confirm(`ยืนยันลบ/ยกเลิกรายการ ${row.item_id} ?`))return;try{await apiDelete(`/history/${row.id}/delete/`);load()}catch(err){setError(err.message)}}return <><PageHeader title="ประวัติการเบิก / รับเข้า (History)" subtitle="จัดกลุ่มตามปีและเดือน · Stock IN / OUT / Adjustment"/><Alert>{error}</Alert><section className="panel"><div className="tab-row history-tabs">{[["ALL","ทั้งหมด"],["IN","รับเข้า"],["OUT","เบิกออก"],["ADJUSTMENT","ปรับยอด"]].map(([v,l])=><button key={v} className={`tab ${type===v?"active":""}`} onClick={()=>setType(v)}>{l}</button>)}</div><div className="toolbar wrap"><input className="search-input" value={q} onChange={e=>setQ(e.target.value)} placeholder="ค้นหา Part ID / Part / Machine..."/><select value={requester} onChange={e=>setRequester(e.target.value)}><option value="">ผู้เบิกทั้งหมด</option>{(options.employees||[]).map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><select value={recorder} onChange={e=>setRecorder(e.target.value)}><option value="">ผู้บันทึกทั้งหมด</option>{(options.employees||[]).map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><button className="btn ghost" onClick={load}>ค้นหา</button></div>{loading?<div className="empty">กำลังโหลด...</div>:groups.length===0?<div className="empty">ไม่พบข้อมูล</div>:<div className="history-groups">{groups.map(y=><section className="history-year" key={y.year}><button className="history-collapse year" onClick={()=>setOpenYears(x=>({...x,[y.year]:!x[y.year]}))}><span>{openYears[y.year]?"▾":"▸"} ปี {y.year}</span><b>{y.months.reduce((n,m)=>n+m.rows.length,0)} รายการ</b></button>{openYears[y.year]&&<div className="history-months">{y.months.map(m=><section className="history-month" key={m.key}><button className="history-collapse month" onClick={()=>setOpenMonths(x=>({...x,[m.key]:!x[m.key]}))}><span>{openMonths[m.key]?"▾":"▸"} {m.label}</span><b>{m.rows.length} รายการ</b></button>{openMonths[m.key]&&<HistoryTable rows={m.rows} auth={auth} onEdit={setEdit} onDelete={remove}/>}</section>)}</div>}</section>)}</div>}</section>{edit&&<EditHistory row={edit} options={options} onClose={()=>setEdit(null)} onSaved={()=>{setEdit(null);load()}}/>}</>}
