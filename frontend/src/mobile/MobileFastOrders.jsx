import { useEffect, useMemo, useState } from "react";
import { apiDelete, apiGet, apiPost } from "../api";
import { useAuth } from "../auth";
import { Alert, Modal } from "../components/Common";
import { FastOrderModal } from "../pages/FastOrders";
import { MobileEmpty, MobileLoading, MobilePage, MobileSearch } from "./MobileCommon";
function Quick({row,onClose,onDone}){const[amount,setAmount]=useState(1),[busy,setBusy]=useState(false),[error,setError]=useState("");async function submit(e){e.preventDefault();const n=Number(amount);if(!Number.isInteger(n)||n<=0){setError("จำนวนต้องเป็นจำนวนเต็มมากกว่า 0");return}setBusy(true);try{const r=await apiPost(`/fast-orders/${row.id}/order/`,{amount:n});onDone(r)}catch(e){setError(e.message)}finally{setBusy(false)}}return <Modal title="Fast Order" onClose={onClose}><form onSubmit={submit}><div className="m-quick-summary"><b>{row.item_id}</b><h3>{row.part_name}</h3><p>{row.machine_code} · {row.machine_name}</p><span>JOB = SPARE</span></div><label className="field"><span>จำนวน *</span><input autoFocus type="number" min="1" step="1" value={amount} onChange={e=>setAmount(e.target.value)}/></label><Alert>{error}</Alert><button className="m-confirm-btn receive" disabled={busy}>{busy?"กำลังเพิ่ม...":"เพิ่มเข้า Order"}</button></form></Modal>}
export default function MobileFastOrders(){const auth=useAuth();const[rows,setRows]=useState([]),[q,setQ]=useState(""),[loading,setLoading]=useState(true),[error,setError]=useState(""),[msg,setMsg]=useState(""),[quick,setQuick]=useState(null),[editor,setEditor]=useState(undefined);async function load(force=false){setLoading(true);setError("");try{const d=await apiGet("/fast-orders/",{forceRefresh:force});setRows(d.results||[])}catch(e){setError(e.message)}finally{setLoading(false)}}useEffect(()=>{load()},[]);const shown=useMemo(()=>rows.filter(x=>!q||`${x.item_id} ${x.part_name} ${x.machine_code} ${x.machine_name} ${x.name}`.toLowerCase().includes(q.toLowerCase())),[rows,q]);async function remove(x){if(!confirm(`ยืนยันลบ Fast Order ${x.name||x.item_id}?`))return;try{await apiDelete(`/fast-orders/${x.id}/`);await load(true)}catch(e){setError(e.message)}}return <MobilePage title="Fast Order" subtitle="SPARE แบบกดเร็ว" actions={<div className="m-page-actions">{auth.can("can_add_order")&&<button className="m-icon-action" onClick={()=>setEditor(null)}>＋</button>}<button className="m-icon-action" onClick={()=>load(true)}>↻</button></div>}><Alert>{error}</Alert>{msg&&<Alert type="success">{msg}</Alert>}<MobileSearch value={q} onChange={setQ} placeholder="Item / Part / Machine..."/>{loading?<MobileLoading/>:shown.length===0?<MobileEmpty/>:<div className="m-card-list">{shown.map(x=><article className="m-fast-card m-fast-card-full" key={x.id}><div><span className="m-factory">{x.factory==="MM-11"?"Phase11":"Phase4"}</span><b>{x.item_id}</b><h3>{x.part_name}</h3><p>{x.machine_code} · {x.machine_name}</p>{x.remark&&<small>{x.remark}</small>}</div><div className="m-fast-actions"><button className="m-fast-button" onClick={()=>setQuick(x)}>⚡ สั่ง</button>{auth.can("can_add_order")&&<button onClick={()=>setEditor(x)}>แก้ไข</button>}{auth.can("can_add_order")&&<button className="danger" onClick={()=>remove(x)}>ลบ</button>}</div></article>)}</div>}{quick&&<Quick row={quick} onClose={()=>setQuick(null)} onDone={r=>{setQuick(null);setMsg(`เพิ่ม ${r.order_number} แล้ว · ${r.item_id} × ${r.amount}`)}}/>}{editor!==undefined&&<FastOrderModal row={editor} onClose={()=>setEditor(undefined)} onSaved={()=>{setEditor(undefined);load(true)}}/>}</MobilePage>}
