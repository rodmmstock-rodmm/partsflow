import { useEffect, useMemo, useState } from "react";
import { apiGet } from "../api";
import { Alert } from "../components/Common";
import { MobileEmpty, MobileLoading, MobilePage, MobileSearch } from "./MobileCommon";
export default function MobileVendors(){const[rows,setRows]=useState([]),[q,setQ]=useState(""),[loading,setLoading]=useState(true),[error,setError]=useState("");async function load(force=false){setLoading(true);try{const d=await apiGet("/suppliers/",{forceRefresh:force});setRows(d.results||[])}catch(e){setError(e.message)}finally{setLoading(false)}}useEffect(()=>{load()},[]);const shown=useMemo(()=>rows.filter(x=>!q||`${x.code} ${x.name} ${x.contact} ${x.phone}`.toLowerCase().includes(q.toLowerCase())),[rows,q]);return <MobilePage title="Vendor" subtitle="Vendor Master" actions={<button className="m-icon-action" onClick={()=>load(true)}>↻</button>}><Alert>{error}</Alert><MobileSearch value={q} onChange={setQ} placeholder="Vendor Code / Name / Contact..."/>{loading?<MobileLoading/>:shown.length===0?<MobileEmpty/>:<div className="m-card-list">{shown.map(x=><article className="m-master-card" key={x.id}><div className="m-row-between"><div><b>{x.code}</b><h3>{x.name}</h3></div><span className={`status ${x.active?"success":"muted"}`}>{x.active?"Active":"Inactive"}</span></div><div className="m-info-grid"><span>Contact<b>{x.contact||"-"}</b></span><span>Phone<b>{x.phone||"-"}</b></span><span>Email<b>{x.email||"-"}</b></span><span>Lead Time<b>{x.lead_time_days||0} DAY</b></span></div></article>)}</div>}</MobilePage>}
