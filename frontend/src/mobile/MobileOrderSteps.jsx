import { useEffect, useMemo, useState } from "react";
import { apiGet } from "../api";
import { Alert, fmt, money } from "../components/Common";
import { MobileEmpty, MobileLoading, MobilePage, MobileSearch } from "./MobileCommon";
export default function MobileOrderSteps(){const[dept,setDept]=useState("MODIFY"),[projects,setProjects]=useState([]),[selected,setSelected]=useState(null),[detail,setDetail]=useState(null),[q,setQ]=useState(""),[loading,setLoading]=useState(true),[error,setError]=useState("");async function load(force=false){setLoading(true);try{const d=await apiGet(`/order-projects/?department=${dept}`,{forceRefresh:force});setProjects(d.results||[]);setSelected(null);setDetail(null)}catch(e){setError(e.message)}finally{setLoading(false)}}useEffect(()=>{load()},[dept]);async function open(p){setSelected(p);setLoading(true);try{setDetail(await apiGet(`/order-projects/${p.id}/`,{forceRefresh:true}))}catch(e){setError(e.message)}finally{setLoading(false)}}const shown=useMemo(()=>projects.filter(p=>!q||`${p.name} ${p.owner_name||""}`.toLowerCase().includes(q.toLowerCase())),[projects,q]);if(selected&&detail)return <MobilePage title={selected.name} subtitle={`${selected.owner_name||"ไม่ระบุ Owner"} · ${dept}`} actions={<button className="m-back-btn" onClick={()=>{setSelected(null);setDetail(null)}}>← Project</button>}><div className="m-project-summary"><div><span>ราคาอะไหล่ทั้งหมด</span><b>฿ {money(detail.project?.total_order_value||selected.total_order_value||0)}</b></div><div><span>ราคาอะไหล่ที่ใช้</span><b>฿ {money(detail.project?.used_value||selected.used_value||0)}</b></div></div><div className="m-step-list">{(detail.steps||[]).map(s=><section className="m-step" key={s.id}><header><b>Step {s.step_no}</b><span>{(s.orders||[]).length} Orders</span></header>{(s.orders||[]).map(o=><article className="m-step-order" key={o.id}><div className="m-row-between"><b>{o.order_number}</b><span>{o.lifecycle_status||o.status}</span></div><h3>{o.item_id} · {o.part_name}</h3><p>{o.detail||"-"}</p><small>{fmt(o.amount)} {o.unit_text||""} · {o.machine_code||o.machine_name||"-"}</small></article>)}</section>)}</div></MobilePage>;
return <MobilePage title="Order Step" subtitle="Project Modify / Automation" actions={<button className="m-icon-action" onClick={()=>load(true)}>↻</button>}><Alert>{error}</Alert><div className="m-segment"><button className={dept==="MODIFY"?"active":""} onClick={()=>setDept("MODIFY")}>Modify</button><button className={dept==="AUTOMATION"?"active":""} onClick={()=>setDept("AUTOMATION")}>Automation</button></div><MobileSearch value={q} onChange={setQ} placeholder="Project / Owner..."/>{loading?<MobileLoading/>:shown.length===0?<MobileEmpty/>:<div className="m-card-list">{shown.map(p=><button className="m-project-card" key={p.id} onClick={()=>open(p)}><div><b>{p.name}</b><span>{p.owner_name||"ไม่ระบุ Owner"}</span></div><div><strong>฿ {money(p.total_order_value||0)}</strong><small>{p.step_count||0} Steps</small></div></button>)}</div>}</MobilePage>}
