import { useEffect,useRef,useState } from"react";import{useNavigate}from"react-router-dom";import{apiGet}from"../api";import{useAuth}from"../auth";import{Alert,PartImage,fmt,stockDisplay}from"../components/Common";import PartDetailModal from"../components/PartDetailModal";import BarcodeScannerModal from"../components/BarcodeScannerModal";import{useOptions}from"../optionsContext";import{PartModal}from"../pages/Dashboard";import{MobileAdjustModal,MobileEmpty,MobileLoading,MobilePage,MobileSearch,MobileStockModal,StockBadge}from"./MobileCommon";
export default function MobileDashboard(){const auth=useAuth(),navigate=useNavigate(),{options}=useOptions();const[kpi,setKpi]=useState({}),[parts,setParts]=useState([]),[q,setQ]=useState(""),[dq,setDq]=useState(""),[warehouse,setWarehouse]=useState(""),[view,setView]=useState("active"),[page,setPage]=useState(1),[meta,setMeta]=useState({}),[loading,setLoading]=useState(true),[error,setError]=useState(""),[stock,setStock]=useState(null),[adjust,setAdjust]=useState(null),[edit,setEdit]=useState(null),[detail,setDetail]=useState(null),[scannerOpen,setScannerOpen]=useState(false),timer=useRef(null);useEffect(()=>{clearTimeout(timer.current);timer.current=setTimeout(()=>{setPage(1);setDq(q.trim())},300);return()=>clearTimeout(timer.current)},[q]);async function load(force=false){setLoading(true);setError("");try{const p=new URLSearchParams({page:String(page),page_size:"20",active:view==="active"?"true":"false"});if(dq)p.set("q",dq);if(warehouse)p.set("warehouse",warehouse);const[d,r]=await Promise.all([apiGet("/dashboard/",{forceRefresh:force}),apiGet(`/parts/?${p}`,{forceRefresh:force})]);setKpi(d.kpi||{});setParts(r.results||[]);setMeta(r||{})}catch(e){setError(e.message)}finally{setLoading(false)}}useEffect(()=>{load()},[page,dq,warehouse,view]);return <MobilePage title="Stock" subtitle="ค้นหา · เบิก · รับเข้า · ปรับยอด แบบมือถือ" actions={<button className="m-icon-action" onClick={()=>load(true)}>↻</button>}><Alert>{error}</Alert><div className="m-kpi-row"><div><span>Parts</span><b>{fmt(kpi.parts)}</b></div><button type="button" className="danger m-kpi-link" onClick={()=>navigate("/safety-stock")} aria-label="เปิดหน้า Safety Stock"><span>ต่ำกว่า Min</span><b>{fmt(kpi.safety_stock)}</b></button><button type="button" className="warning m-kpi-link" onClick={()=>navigate("/orders")} aria-label="เปิดหน้า Order สำหรับ Safety Stock ที่กำลังสั่ง"><span>กำลัง Order</span><b>{fmt(kpi.safety_stock_ordered)}</b></button></div><button className="m-dashboard-spare-set" onClick={()=>navigate("/spare-sets")}><span>⚙</span><div><b>ชุดอะไหล่เครื่องจักร</b><small>Machine Spare Sets</small></div><strong>›</strong></button><div style={{display:"grid",gridTemplateColumns:"minmax(0,1fr) 42px",gap:7,alignItems:"stretch"}}><MobileSearch value={q} onChange={setQ} placeholder="Part ID / Part / Detail / Location..."/><button type="button" className="m-icon-action" title="สแกน Barcode / QR Code" aria-label="สแกน Barcode หรือ QR Code" onClick={()=>setScannerOpen(true)} style={{width:42,minWidth:42,fontSize:19}}>⌗</button></div><div className="m-filter-row"><select value={warehouse} onChange={e=>{setWarehouse(e.target.value);setPage(1)}}><option value="">ทุกคลัง</option><option value="MM-4">Phase4</option><option value="MM-11">Phase11</option></select><div className="m-segment"><button className={view==="active"?"active":""} onClick={()=>{setView("active");setPage(1)}}>Active</button><button className={view==="inactive"?"active":""} onClick={()=>{setView("inactive");setPage(1)}}>Inactive</button></div></div>{loading?<MobileLoading/>:parts.length===0?<MobileEmpty/>:<div className="m-card-list">{parts.map(p=><article className="m-part-card" key={p.id} onClick={()=>setDetail(p)} onKeyDown={e=>{if(e.target!==e.currentTarget)return;if(e.key==="Enter"||e.key===" "){e.preventDefault();setDetail(p)}}} role="button" tabIndex={0} title="แตะเพื่อดูรายละเอียด" style={{cursor:"pointer"}}><div className="m-part-card-top"><div className="m-card-thumb"><PartImage src={p.image_url||p.image_path} fallbackSrc={p.image_fallback_url} alt={p.name}/></div><div className="m-card-main"><div className="m-card-code">{p.sku}</div><h3>{p.name}</h3><p>{p.description||"-"}</p></div>{view==="inactive"?<span className="status muted">Inactive</span>:<StockBadge row={p}/>}</div><div className="m-part-meta"><span>📍 {p.location_code||"-"}</span><span>{p.warehouse_label||"-"}</span><span>{p.maker_name||"-"}</span></div><div className="m-stock-line"><span>คงเหลือ</span><strong>{stockDisplay(p)} <small>{p.unit_code}</small></strong><span>Min {fmt(p.min_stock)}</span></div>{(auth.can("can_edit_parts")||(view==="active"&&(auth.can("can_issue_stock")||auth.can("can_receive_stock")||auth.can("can_adjust_stock"))))&&<div className="m-action-grid" onClick={e=>e.stopPropagation()}>{auth.can("can_edit_parts")&&<button type="button" className="m-action edit" onClick={e=>{e.stopPropagation();setEdit(p)}}>✎ แก้ไข</button>}{view==="active"&&auth.can("can_issue_stock")&&<button className="m-action issue" onClick={e=>{e.stopPropagation();setStock({mode:"issue",part:p})}}>− เบิก</button>}{view==="active"&&auth.can("can_receive_stock")&&<button className="m-action receive" onClick={e=>{e.stopPropagation();setStock({mode:"receive",part:p})}}>＋ รับเข้า</button>}{view==="active"&&auth.can("can_adjust_stock")&&<button type="button" className="m-action adjust" onClick={e=>{e.stopPropagation();setAdjust(p)}}>± ปรับยอด</button>}</div>}</article>)}</div>}<div className="m-pagination"><button disabled={!meta.has_previous||loading} onClick={()=>setPage(x=>Math.max(1,x-1))}>← ก่อนหน้า</button><span>{meta.page||1} / {meta.total_pages||1}</span><button disabled={!meta.has_next||loading} onClick={()=>setPage(x=>x+1)}>ถัดไป →</button></div>{scannerOpen&&<BarcodeScannerModal onClose={()=>setScannerOpen(false)} onDetected={value=>{setQ(value);setDq(value);setPage(1);setScannerOpen(false)}}/>}{stock&&<MobileStockModal {...stock} employee={auth.employee} onClose={()=>setStock(null)} onSaved={async()=>{setStock(null);await load(true)}}/>}{adjust&&<MobileAdjustModal part={adjust} employee={auth.employee} onClose={()=>setAdjust(null)} onSaved={async()=>{setAdjust(null);await load(true)}}/>}{edit&&<PartModal part={edit} options={options||{}} onClose={()=>setEdit(null)} onSaved={async()=>{setEdit(null);setDetail(null);await load(true)}}/>}{detail&&<PartDetailModal part={detail} onClose={()=>setDetail(null)}/>}</MobilePage>}

