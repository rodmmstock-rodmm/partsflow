import { useMemo, useState } from "react";
import { apiPatch, apiPost } from "../api";
import { Alert, Modal } from "./Common";
const partLabel=p=>`${p.sku} · ${p.name}`,machineLabel=m=>`${m.code} · ${m.name}`;
const findMachine=(xs,v)=>{const t=String(v||"").trim().toLowerCase();return(xs||[]).find(m=>machineLabel(m).toLowerCase()===t||String(m.code).toLowerCase()===t||String(m.name).toLowerCase()===t)};
const findPart=(xs,v)=>{const t=String(v||"").trim().toLowerCase();return(xs||[]).find(p=>partLabel(p).toLowerCase()===t||String(p.sku).toLowerCase()===t)};
export default function SpareSetModal({spareSet,options,onClose,onSaved}){const edit=!!spareSet?.id,machines=options.machines||[],parts=options.parts||[];const initialMachine=machines.find(x=>String(x.id)===String(spareSet?.machine_id||""));const initialItems=(spareSet?.items||[]).map(x=>({partText:`${x.sku} · ${x.name}`,part_id:x.part_id,quantity:x.quantity||1,remark:x.remark||""}));const[form,setForm]=useState({machineText:initialMachine?machineLabel(initialMachine):(spareSet?.machine_code?`${spareSet.machine_code} · ${spareSet.machine_name||""}`.trim():""),machine_id:spareSet?.machine_id||"",name:spareSet?.name||"",description:spareSet?.description||"",active:spareSet?.active!==false,items:initialItems.length?initialItems:[{partText:"",part_id:"",quantity:1,remark:""}]});const[busy,setBusy]=useState(false),[error,setError]=useState("");const duplicateIds=useMemo(()=>{const seen=new Set(),dup=new Set();form.items.forEach(x=>{if(!x.part_id)return;seen.has(x.part_id)?dup.add(x.part_id):seen.add(x.part_id)});return dup},[form.items]);const set=(k,v)=>setForm(x=>({...x,[k]:v}));const updateItem=(i,p)=>setForm(x=>({...x,items:x.items.map((r,n)=>n===i?{...r,...p}:r)}));const changeMachine=v=>{const m=findMachine(machines,v);setForm(x=>({...x,machineText:v,machine_id:m?.id||""}))};const changePart=(i,v)=>{const p=findPart(parts,v);updateItem(i,{partText:v,part_id:p?.id||""})};const addItem=()=>setForm(x=>({...x,items:[...x.items,{partText:"",part_id:"",quantity:1,remark:""}]}));const removeItem=i=>setForm(x=>({...x,items:x.items.filter((_,n)=>n!==i)}));async function save(e){e.preventDefault();setError("");if(!form.machine_id)return setError("กรุณาเลือกเครื่องจักร");if(!form.name.trim())return setError("กรุณาระบุชื่อ Set");if(form.items.some(x=>!x.part_id))return setError("กรุณาเลือกอะไหล่จากรายการให้ครบ");if(duplicateIds.size)return setError("มีอะไหล่ซ้ำใน Set");if(form.items.some(x=>Number(x.quantity)<=0))return setError("จำนวนต่อ Set ต้องมากกว่า 0");const payload={machine_id:form.machine_id,name:form.name.trim(),description:form.description.trim(),active:!!form.active,items:form.items.map(x=>({part_id:x.part_id,quantity:x.quantity,remark:x.remark||""}))};setBusy(true);try{onSaved(edit?await apiPatch(`/spare-sets/${spareSet.id}/`,payload):await apiPost("/spare-sets/",payload))}catch(err){setError(err.message)}finally{setBusy(false)}}return <Modal title={edit?"แก้ไข Machine Spare Set":"สร้าง Machine Spare Set"} onClose={onClose} wide><form onSubmit={save}><div className="form-grid three"><label className="field span2"><span>Machine *</span><input list="spare-set-machines" value={form.machineText} onChange={e=>changeMachine(e.target.value)} placeholder="พิมพ์ Machine Code / Machine Name..." autoComplete="off" required/><small className="field-help">พิมพ์ค้นหา แล้วเลือกเครื่องจักรจากรายการ</small></label><label className="check"><input type="checkbox" checked={form.active} onChange={e=>set("active",e.target.checked)}/> Active</label><label className="field span3"><span>ชื่อ Set *</span><input value={form.name} onChange={e=>set("name",e.target.value)} required/></label><label className="field span3"><span>รายละเอียด Set</span><textarea rows="2" value={form.description} onChange={e=>set("description",e.target.value)}/></label></div><datalist id="spare-set-machines">{machines.map(m=><option key={m.id} value={machineLabel(m)}/>)}</datalist><div className="section-head spare-set-items-head"><div><h2>รายการอะไหล่ใน Set</h2><p>เลือกอะไหล่และกำหนดจำนวนที่ใช้ต่อ 1 Set</p></div><button type="button" className="btn ghost" onClick={addItem}>+ เพิ่มอะไหล่</button></div><div className="spare-set-editor-list">{form.items.map((row,index)=><div className={`spare-set-editor-row ${row.part_id&&duplicateIds.has(row.part_id)?"duplicate":""}`} key={index}><div className="spare-set-editor-no">{index+1}</div><label className="field spare-set-part-field"><span>อะไหล่ *</span><input list="spare-set-parts" value={row.partText} onChange={e=>changePart(index,e.target.value)} placeholder="Item ID · Part Name"/></label><label className="field"><span>จำนวน / Set *</span><input type="number" min="0.01" step="0.01" value={row.quantity} onChange={e=>updateItem(index,{quantity:e.target.value})}/></label><label className="field"><span>Remark</span><input value={row.remark} onChange={e=>updateItem(index,{remark:e.target.value})}/></label><button type="button" className="mini danger spare-set-remove" onClick={()=>removeItem(index)} disabled={form.items.length===1}>ลบ</button></div>)}</div><datalist id="spare-set-parts">{parts.map(p=><option key={p.id} value={partLabel(p)}/>)}</datalist><Alert>{error}</Alert><div className="modal-actions"><button type="button" className="btn ghost" onClick={onClose}>ยกเลิก</button><button className="btn primary" disabled={busy}>{busy?"กำลังบันทึก...":"บันทึก Set"}</button></div></form></Modal>}
