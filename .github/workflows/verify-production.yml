name: Deprecated production verification

# Incident-only workflow retired after the 2026 Order import was verified.
# Kept inert because the connected GitHub action cannot delete this workflow file.
on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Production verification workflow retired."
