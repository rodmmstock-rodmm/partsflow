name: Production Diagnostic

# Retired: the /api/production-check/, /api/production-repair/, and
# /api/production-session-check/ endpoints this workflow depended on have
# been removed from the backend (backend/core/production_check_api.py was
# a one-time repair tool for the June 2026 Order import incident, long
# since resolved, and was exposing a hardcoded secret key once this repo
# became public). Kept inert rather than deleted, matching the pattern
# used for verify-production.yml.
on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Production diagnostic workflow retired - see git history for details."
